$TTL	604800
@	IN	SOA	{{ansible_hostname}}.{{domain_name}} root.{{ansible_hostname}}.{{domain_name}} (
			      2		; Serial
			 604800		; Refresh
			  86400		; Retry
			2419200		; Expire
			 604800 )	; Negative Cache TTL
;
@		IN	NS		{{ansible_hostname}}.
@		IN	A		{{ansible_default_ipv4.address}}
@		IN	AAAA	::1



forum.{{domain_name}}.	IN 	A	{{ansible_default_ipv4.address}}
apache.{{domain_name}}.	IN	A	{{ansible_default_ipv4.address}}
